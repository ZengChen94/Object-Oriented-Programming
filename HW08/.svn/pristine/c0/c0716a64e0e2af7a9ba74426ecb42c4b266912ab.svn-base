package model.datapacket;

import java.awt.Container;
import java.rmi.RemoteException;

import javax.swing.JLabel;

import common.ICmd2ModelAdapter;
import commonG14.DataPacketChatRoom;
import commonG14.IUser;
import model.ILocalHostModelAdapter;
import provided.datapacket.ADataPacketAlgoCmd;

public class SendStringCmd<D> extends ADataPacketAlgoCmd<String, D, IUser, ICmd2ModelAdapter, DataPacketChatRoom<D>>{

	private ICmd2ModelAdapter cmd2ModelAdapter;
	
	@Override
	public String apply(Class<?> index, DataPacketChatRoom<D> host, IUser... params) {
		String data = (String) host.getData();
		IUser user = host.getSender();
		Container panel = cmd2ModelAdapter.getContainer();
		try {
			JLabel label = new JLabel(user.getName() + "@" + user.getUUID() + ":" + data);
			panel.add(panel);
		} catch (RemoteException e) {
			e.printStackTrace();
		}
		return data;
	}

	@Override
	public void setCmd2ModelAdpt(ICmd2ModelAdapter cmd2ModelAdpt) {
		// TODO Auto-generated method stub
		this.cmd2ModelAdapter = cmd2ModelAdpt;
	}

}
