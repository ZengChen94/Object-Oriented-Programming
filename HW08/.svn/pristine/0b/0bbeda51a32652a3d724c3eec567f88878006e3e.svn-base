package model.datapacket;

import commonG14.DataPacketChatRoom;
import commonG14.IUser;
import model.ILocalHostModelAdapter;
import provided.datapacket.ADataPacketAlgoCmd;

public class LeaveCmd<D> extends ADataPacketAlgoCmd<Boolean, D, IUser, ILocalHostModelAdapter, DataPacketChatRoom<D>> {

	@Override
	public Boolean apply(Class<?> index, DataPacketChatRoom<D> host, IUser... params) {
		if (params.length != 1) {
			throw new IllegalArgumentException("user number incorrect");
		}
		IUser user = params[0];
		return null;
	}

	@Override
	public void setCmd2ModelAdpt(ILocalHostModelAdapter cmd2ModelAdpt) {
		// TODO Auto-generated method stub
		
	}
	
}
