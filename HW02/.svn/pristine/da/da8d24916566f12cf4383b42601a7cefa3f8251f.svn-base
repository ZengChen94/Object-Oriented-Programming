package balls;

import java.awt.Color;
import java.awt.Component;
import java.awt.Point;

import util.Randomizer;

public class BreathingBall extends ABall{
	private final int total = 20;
	private int cnt = total;
	
	public BreathingBall(Point location, int radius, Point velocity, Color color, Component canvas) {
		super(location, radius, velocity, color, canvas);
	}
	
	@Override
	public void specifyBall() {
		cnt--;
		if(cnt == 0){
			set_radius(Randomizer.Singleton.randomInt(10, 30));
			cnt = total;
		}
	}
}
