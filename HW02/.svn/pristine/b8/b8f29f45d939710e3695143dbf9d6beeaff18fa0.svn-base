package balls;

import java.awt.Color;
import java.awt.Component;
import java.awt.Point;

import util.Randomizer;

public class CurveBall extends ABall{
//	private double angle = Randomizer.Singleton.randomInt(0, 30) * Math.PI / 180;
	
	public CurveBall(Point location, int radius, Point velocity, Color color, Component canvas) {
		super(location, radius, velocity, color, canvas);
	}
	
	@Override
	public void specifyBall() {
//		int velocity_x_pre = velocity.x;
//		int velocity_y_pre = velocity.y;
//		velocity.x = (int)Math.round(velocity_x_pre * Math.cos(angle) - velocity_y_pre * Math.sin(angle));
//		velocity.y = (int)Math.round(velocity_y_pre * Math.cos(angle) + velocity_x_pre * Math.sin(angle));
	}
}
